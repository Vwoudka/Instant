# Backend address (used for both REST calls and the WebSocket endpoint)
REACT_APP_API_URL=http://localhost:5000

# Optional: set explicitly if the WebSocket URL differs from the API URL
REACT_APP_WS_URL=ws://localhost:5000/ws
